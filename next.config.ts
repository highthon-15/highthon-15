import type {NextConfig} from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'cnqnq6x1162u.objectstorage.ap-seoul-1.oci.customer-oci.com',
      'img.freepik.com',
    ],
  },
};

export default nextConfig;
